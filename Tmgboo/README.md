# telegram_bot_full

این پروژه یک قالب کامل برای اجرای یک بات تلگرام (دو زبانه) است — قابل دیپلوی روی Render.

**تذکر:** توکن بات را خودت در متغیر محیطی `BOT_TOKEN` در Render یا در فایل `config/settings.json` قرار بده.
